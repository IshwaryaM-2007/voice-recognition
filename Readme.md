# AI-Generated Voice Detection API

This project implements a FastAPI-based service to detect
whether a given voice sample is AI-generated or human-generated.

The API accepts Base64-encoded audio and returns a structured
JSON response with classification and confidence score.

Built for GUVI Hackathon submission.